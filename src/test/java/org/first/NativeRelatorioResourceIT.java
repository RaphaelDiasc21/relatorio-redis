package org.first;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeRelatorioResourceIT extends RelatorioResourceTest {

    // Execute the same tests but in native mode.
}